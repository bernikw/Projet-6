{% extends 'base.html.twig' %}

{% block title %} SnowTricks - Réinitialiser votre mot de passe {% endblock %}

{% block body %}
 <section class="container my-5">
    <div class="form-group row">
        <div class"col">
        <h1 class="text-center h3 mb-3 font-weight-normal">Entrez votre nouveau mot de passe</h1>
             <div class="col-lg-8 mx-auto">
             {{ form_start(resetForm) }}

             <div class="mb-3">
                 {{ form_row(resetForm.password.first) }}
                </div>
            <div class="mb-3">
                 {{ form_row(resetForm.password.second) }}
                </div>
                
        <div class="d-grid gap-2 col-3 mx-auto my-5">
        <button type="submit" class="btn btn-lg btn-primary mt-3" style="background-color: #45637d;">Envoyer</button>
        </div>
             {{ form_end(resetForm) }}
             </div>
            </div>
        </div>
    </section>
{% endblock %}