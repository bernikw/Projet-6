<nav class="navbar navbar-expand-lg navbar-dark" style="background-color: #45637d;">
	<div class="container-fluid">
		<a class="navbar-brand" href="{{path('app_home')}}">SnowTricks</a>
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>

		<div class="collapse navbar-collapse" id="navbarColor01">
			<ul class="navbar-nav me-auto">
				<li class="nav-item">
					<a class="nav-link active" href="{{ path('app_home') }}">Accueil
						<span class="visually-hidden">(current)</span>
					</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="{{ path('app_trick_app_list') }}">Tricks</a>
				</li>
			</ul>


			{% if app.user %}

				<a href="#" role="button" class="btn btn btn-outline-light px-3 me-2 px-3 me-2">Bienvenue
					{{ app.user.pseudo}}</a>

				<a href="{{ path('app_trick_app_create')}}" role="button" class="btn btn btn-outline-light px-3 me-2 px-3 me-2">Ajouter un trick</a>

				<a href="{{ path('app_logout') }}" role="button" class="btn btn-outline-light px-3 me-2">Me déconnecter</a>

			{% else %}

				<a href="{{ path('app_login') }}" role="button" class="btn btn-outline-light px-3 me-2">Se connecter</a>

				<a href="{{ path('app_register') }}" role="button" class="btn btn-light me-3">S'inscrire</a>

			{% endif %}

			<a class="btn btn-light px-3" href="https://github.com/bernikw/Projet-6" role="button" target="_blank">
				<i class="bi bi-github" style="font-size: 16px; color: #45637d;"></i></a>
		</div>
	</div>
</div></nav>
